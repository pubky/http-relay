# Agent Strategy

- Use agents liberally to keep main context window clean
- Offload research, exploration, and parallel analysis to agents
- For complex problems, throw more compute at it via agents
- One task per agent: a 5-item task list means spawn 5 agents in parallel